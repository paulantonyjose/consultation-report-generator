{
    "version": 2,
    "builds": [
        {
            "src": "app.py",
            "use": "@vercel/python",
            "config": {
               "maxLambdaSize": "15mb",
               "installCommand": "sudo apt install -y apt install python3-pip pango && pip install --no-cache-dir -r requirements.txt && pip3 install --no-cache-dir -r requirements.txt"
         }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "app.py"
        }
    ]
}
