{
    "version": 2,
    "builds": [
        {
            "src": "app.py",
            "use": "@vercel/python",
            "config": {
               "maxLambdaSize": "15mb",
               "installCommand": "apt install python3-pip libpango-1.0-0 libpangoft2-1.0-0 libjpeg-dev libopenjp2-7-dev libffi-dev && conda install weasyprint && pip install --no-cache-dir -r requirements.txt && pip3 install --no-cache-dir -r requirements.txt"
         }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "app.py"
        }
    ]
}
