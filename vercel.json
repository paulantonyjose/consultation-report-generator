{
    "version": 2,
    "builds": [
        {
            "src": "app.py",
            "use": "@vercel/python",
            "config": {
               "maxLambdaSize": "15mb",
               "installCommand": "dnf install python3-pip pango gcc python3-devel gcc-c++ zlib-devel libjpeg-devel openjpeg2-devel libffi-devel && pip install --no-cache-dir -r requirements.txt && pip3 install --no-cache-dir -r requirements.txt"
         }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "app.py"
        }
    ]
}
