{
    "version": 2,
    "builds": [
        {
            "src": "app.py",
            "use": "@vercel/python",
            "config": {
               "maxLambdaSize": "15mb",
               "installCommand": "sudo apt install -y libpango-1.0-0 libharfbuzz0b libpangoft2-1.0-0 && pip install --no-cache-dir -r requirements.txt && pip3 install --no-cache-dir -r requirements.txt"
         }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "app.py"
        }
    ]
}
